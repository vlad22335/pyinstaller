import logging

logging.basicConfig(filename='log.txt', level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

logging.info('Привет, это логгер!')

try:
    # Ваш код здесь
    pass
except Exception as e:
    logging.error(f'Произошла ошибка: {str(e)}')
